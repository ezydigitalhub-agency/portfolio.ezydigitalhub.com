export interface SocialCard {
  id: string;
  name: string;
  subName: string;
  icon: string;
  iconBg: string;
  title: string;
  description: string;
  linkText: string;
  color: string;
}

export interface PillarDetail {
  id: string;
  title: string;
  tagline: string;
  icon: any;
  bulletPoints: string[];
  metrics: string[];
}
